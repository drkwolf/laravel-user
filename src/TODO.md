# Abstract The validaiton rules
add the possiblity to merge rules,
for example USeroptoins, HasContact have thier own rules

## api definition 

# the simplest way
``` php

public function rules () {
    return [
        /// define your rules
        $this->userOptions
    ];
}